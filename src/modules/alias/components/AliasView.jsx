import styled from 'styled-components/macro';

import { View } from '@vkontakte/vkui';

const AliasView = styled(View)`
  .ModalRoot__mask {
    background: rgba(62, 39, 35, 0.5);
  }
`;

export default AliasView;
